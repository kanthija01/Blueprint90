"""Module seed files — one file per module.

Each file exports a `MODULE: Module` instance.  Seeders MUST not contain any
LLM-generated content; every fact comes from `fitness_database.docx` (the 10 originals)
or `new_modules.md` (the 4 Claude-authored extensions, flagged `is_authored_extension=True`).
"""
