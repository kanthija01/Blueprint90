"""CONSISTENCY CODE (Consistency Failure - Women) — original module from fitness_database.docx.

CONTENT PENDING: see seed/modules/pcos.py for the stub policy.
"""
from models.module import Module


MODULE = Module(
    slug="consistency_code",
    display_name="Consistency Code",
    is_authored_extension=False,
    content_pending=True,
)
