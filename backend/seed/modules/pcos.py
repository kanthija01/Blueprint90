"""PCOS — original module from fitness_database.docx.

CONTENT PENDING: the actual content for this module lives in the user's
fitness_database.docx, which has not yet been provided. This file is a
structural stub so the rules engine and seed pipeline have a known module
to reference. `content_pending=True` makes the gap explicit; `seed_all.py`
WILL warn on every stub it inserts.

To populate: provide fitness_database.docx (or paste the PCOS section)
and the content arrays below will be filled in.
"""
from models.module import Module


MODULE = Module(
    slug="pcos",
    display_name="PCOS",
    is_authored_extension=False,
    content_pending=True,
)
